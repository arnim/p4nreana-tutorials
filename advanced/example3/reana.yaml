version: 0.9.0
inputs:
  parameters:
    outputfile: greetings.txt
  files:
    - reduce.py
workflow:
  type: serial
  specification:
    steps:
      - environment: 'gitlab-p4n.aip.de:5005/p4nreana/reana-env:py311-astro-ml.10043'
        commands:
          - env >> ${outputfile}
          - echo "Hello from reana">>${outputfile}
          - python reduce.py 
outputs:
  files:
   - greetings.txt
   - projections_comparison.png
